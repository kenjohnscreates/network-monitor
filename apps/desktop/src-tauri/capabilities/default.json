{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the desktop app's main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
